server:
    port: 8040
    servlet:
        session:
            cookie:
                name: SESSION
                domain: .bob.com   # 注意有个点，表示所有子域共享
spring:
    main:
        allow-circular-references: true
    datasource:
        url: jdbc:mysql://192.168.101.65:3306/mall-pms?useSSL=false&serverTimezone=UTC
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
    cloud:
        nacos:
            discovery:
                server-addr: 192.168.101.65:8848
#                ip: 168.4.77.65 # public local net, needs to be modified constantly
                port: 8040
    application:
        name: mall-product
    # Server 2 Client date format
    jackson:
        date-format: yyyy-MM-dd HH:mm:ss
    thymeleaf:
        cache: false    # 关闭thymeleaf的缓存
    redis:
        host: 192.168.101.65
        port: 6379
    session:
        store-type: redis
        redis:
            namespace: spring:session
    cache:
        type: redis
        redis:
            time-to-live: 60000 # ms
#            key-prefix: bob_
            cache-null-values: true # avoid cache penetration

mybatis-plus:
    mapper-locations: classpath*:/mapper/**/*.xml
    global-config:
        db-config:
            id-type: auto
            logic-delete-value: 1
            logic-not-delete-value: 0


