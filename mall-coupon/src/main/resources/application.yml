server:
    port: 8010

spring:
    application:
        name: mall-coupon
    datasource:
        url: jdbc:mysql://3.150.39.20:3306/mall-sms?useSSL=false&serverTimezone=UTC
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
    mapper-locations: classpath*:/mapper/**/*.xml
    global-config:
        db-config:
            id-type: auto

